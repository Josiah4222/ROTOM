# Production Environment Configuration for ROTOM Ethiopia
# Copy this file to .env on the production server and fill in the values

# SECURITY WARNING: Generate a new SECRET_KEY for production!
# Run: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=REPLACE_WITH_NEW_SECRET_KEY

# SECURITY WARNING: Set DEBUG to False in production!
DEBUG=False

# Add your server IP and domain names (comma-separated, no spaces)
ALLOWED_HOSTS=178.104.213.200,rotomethiopia.org,www.rotomethiopia.org

# Email Configuration (Gmail App Password)
# To get app password: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=rotomethiopia@reachone-touchone.org
EMAIL_HOST_PASSWORD=REPLACE_WITH_APP_PASSWORD
DEFAULT_FROM_EMAIL=ROTOM Ethiopia <rotomethiopia@reachone-touchone.org>

# Payment Configuration
CHAPA_SECRET_KEY=CHASECK-O6jk9SKQlZJcn8d9qib00WLbvzcRJreo
PAYPAL_RECEIVER_EMAIL=REPLACE_WITH_PAYPAL_EMAIL
PAYPAL_TEST=False

# Optional: Force script name for sub-path hosting (leave empty if hosting at root)
FORCE_SCRIPT_NAME=/rotom
